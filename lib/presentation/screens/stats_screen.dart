import 'package:flutter/material.dart';

// Ecran de visualisation des depenses.
// Bonus: integrer un package comme fl_chart ou syncfusion_flutter_charts
// pour des graphiques (camembert, barres, courbes).

class StatsScreen extends StatelessWidget {
  const StatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Graphiques a integrer ici (fl_chart)'),
    );
  }
}
